{
  "name": "passkey-service",
  "version": "0.1.0",
  "bin": {
    "passkey-service": "bin/passkey-service.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "dev": "cdk synth --no-staging > template.yaml\n sam local start-api -t template.yaml",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.154.1",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.154.1",
    "aws-sdk": "^2.1682.0",
    "constructs": "^10.0.0"
  }
}
